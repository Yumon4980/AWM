# AltTabReplacer 使用说明

一款替代 Windows 默认 `Alt+Tab` 的窗口切换器。后台常驻，按下 `Alt+Tab` 即弹出选择器，选中即切换。

---

## 软件简介
![alt text](docs\image.png)
![alt text](docs\image-1.png)
![alt text](docs\image-2.png)

**AltTabReplacer** 是一款 Windows 上的轻量级窗口切换器（C# / .NET 8 / WPF，开源免费），一键接管系统默认的 `Alt+Tab`，把它升级成"带缩略图、可键盘直达、支持分组"的专业切换面板。

### 程序特点

1. **缩略图网格，一目了然**
2. **16 个物理键直达** —— 沿用 Alt-Tab Terminator 的 QWERTY 键位：`1234 / QWER / ASDF / ZXCV`，双手不离主键区即可秒切。
3. **程序组 + 程序组合** —— Chrome 多标签、VS Code 多工程自动折成一组；把 Word+Excel+PPT 拖成组合，一键同时打开/激活。
4. **跨组扁平搜索** —— 按 `/` 直达搜索框，输入即过滤，`Esc` 退出。

### 它适合谁？

- 程序员：IDE / Terminal / 浏览器多窗口频繁切换
- 设计师 / 自媒体：素材图、参考站、剪辑软件来回对照
- 办公族：Office 三件套 + 微信 + 邮箱 + 浏览器
- 任何人：只要觉得系统自带的 `Alt+Tab` 不够用

### 隐私与开源

完全开源，代码可审计；所有配置存储在本地 `%APPDATA%\AltTabReplacer\`，**不上传任何数据、不联网**。

---

## 30 秒快速上手


### 获取程序


### 按 `Alt + Tab`

在任意位置按下 `Alt + Tab`，选择器即出现在屏幕中央，默认选中第 1 项。系统自带的 `Alt+Tab` 切换器被取代，不会再弹出。

### 新手必会的 5 个操作

| 操作 | 按键 / 鼠标 | 效果 |
|---|---|---|
| 唤起 / 关闭选择器 | `Alt + Tab` | 首次按下唤起，再按一次关闭 |
| 选中第 N 项 | `1 2 3 4 / Q W E R / A S D F / Z X C V` | 16 个槽位物理键直达 |
| 上下移动 | `Tab` / `Shift+Tab` 或 `↓` / `↑` | 注意要先松开 `Alt` |
| 确认 | `Enter` 或鼠标左键 | 程序直接切换；程序组进入二级；程序组合打开全部 |
| 搜索 | `/` 进入，`Esc` 退出 | 跨组扁平搜索，输入即过滤 |

> 小贴士：`Alt` 还按着时按 `Tab` 会被识别成热键（等同关闭选择器）。要用 `Tab` 上下移动，先松开 `Alt`。

### 三种对象，分清楚用得更顺

| 对象 | 行为 |
|---|---|
| **程序** | 一个窗口。按下直接切换过去 |
| **程序组** | 容器。按下进入二级，里面放程序和程序组合（同一进程多窗口时自动折叠） |
| **程序组合** | 一组程序（最多 4 个）。按下**同时打开全部**：已开则激活，未开则按记录路径启动 |

### 一分钟进阶：拖拽管理

- 把一行拖到另一行**中部** → 建立**程序组合**（最多 4 个成员）
- 拖到程序组的**中部** → 把这一行**加入该组**
- 拖到行**之间** → 仅排序
- 按住 `Shift` 强制排序，按住 `Ctrl` 强制并入
- 二级把成员拖到顶部面包屑 → **移出该组**
- 左上角 **「+ 新建程序组」** → 创建一个空组，再把别的拖进来即可

完成以上 5 步操作，你已经能覆盖 90% 的日常场景。更多细节（配置说明、规则示例、卸载方式）请继续往下阅读。

---

## 快速开始

### 1. 获取 exe
https://github.com/Yumon4980/AWM


### 2. 开始使用

在任意位置按下 **`Alt + Tab`** 即可唤起选择器：
- 默认选中第 1 个窗口
- 系统自带的 `Alt+Tab` 切换器被取代，不再弹出

---

## 快捷键速查

### 主选择器（一级）

| 按键 | 作用 |
|---|---|
| `Alt + Tab` | 唤起选择器（首次按下） |
| 再按 `Alt + Tab` | 关闭选择器（等同 `Esc`） |
| `1` `2` `3` `4` | 选中第 1–4 项 |
| `Q` `W` `E` `R` | 选中第 5–8 项 |
| `A` `S` `D` `F` | 选中第 9–12 项 |
| `Z` `X` `C` `V` | 选中第 13–16 项 |
| `Tab` / `↓` | 选中下一项（到底循环） |
| `Shift + Tab` / `↑` | 选中上一项 |
| `Enter` | 确认：程序直接切换，程序组进入二级 |
| `Esc` / `Backspace` | 逐级回退（二级 → 一级 → 关闭） |
| `/` | 进入搜索模式（跨组扁平搜索），`Esc` 退出 |
| 鼠标左键点击 | 切换到当前项 |
| 鼠标右键 | 弹出操作菜单（重命名/关闭/删除/解散等） |
| 鼠标拖动 | 拖到行中部=建组/并入；拖到行间=排序 |

### 二级（程序组内）

二级内同样支持 `1QWE / ASDF / ZXCV`、`Tab` 切换、`Enter` 确认、`Esc` 退回一级。

> 用 `Tab` 上下移动前要先**松开 `Alt`**。`Alt` 还按着时按 `Tab` 会被识别成热键，等同关闭选择器。

---

## 三种对象

| 对象 | 行为 |
|---|---|
| **程序** | 一个窗口。按下直接切换过去 |
| **程序组** | 容器。按下进入二级，里面放程序和程序组合 |
| **程序组合** | 一组程序（最多 4 个）。按下**同时打开全部**：已开则激活，未开则用记录的可执行路径启动 |

---

## 分组与组合操作

### 自动折叠
同一进程开了 ≥2 个窗口时，**自动折叠成一个程序组**，只占一个槽位。

### 手动建组
- 把一行**拖到另一行的中部** → 建立程序组合（最多 4 个）
- 目标是程序组 → 把这一行**加入该组**
- 拖到行间 → 仅排序
- 按住 `Shift` 强制排序，按住 `Ctrl` 强制并入

### 新建程序组
- 点击左上角 **「+ 新建程序组」**，输入名字
- 再把程序/程序组合拖到该行中部即可加入

### 解散与删除
- **自动折叠的组**只剩 1 个成员时**自动解散**
- **手动建的组**即使为空也会保留
- 右键程序组 → **删除程序组**：只删容器，组内成员摊回一级
- 右键程序组合 → **解散程序组合**（一级二级都支持）
- 二级中把成员拖到顶部面包屑 → **移出该组**

> 程序组是严格两级结构：组里不能再嵌套程序组。