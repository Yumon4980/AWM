%这个文件用于记录后续需要实现的功能
# 分组功能 ✅ 已实现
当前的选择是123456789qwer，但是我想用的是一个多级的程序组，快捷键应该设置为1234qwerasdfzxcv，这16个按键；然后如果1对应的是一个程序组，那么按1松开后，需要再按1234qwerasdfzxcv选择对应的程序，想要通过拖动实现分组和排序的功能，所以问题就是
1. 如何区分拖动的目的是分组（加入分组和移出分组）还是移动排序
2. 如何在可视化上进行分组的示意
3. 按ESC应该可以取消第一次的按键，比如按下1松开，1是一个程序组，那么按下ESC则撤销按下1这一操作
4. 如果按下1，1对应的是程序组，则需再按一个按键，选择程序组中的程序，如果是程序，则直接选择

实现见 docs/groupplan.md 与 docs/design.md 5.5/5.6

# 右键重命名功能 ✅ 已实现
可以通过右键换出菜单，菜单中的重命名选项，可以重命名程序命或分组名
（另附：分组还能右键"解散分组"）

# bug修复 ✅ 已修复
有部分程序无法正确显示图标
（原因：原来只用 WM_GETICON，很多程序不响应。改为四级兜底：
 WM_GETICON → GetClassLongPtr → ExtractIconEx → SHGetFileInfo。见 docs/design.md 5.7）
